spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/daimengying/springcloud-config-repo/
#          username : daimengying
#          password : dmy510220

server:
  port: 2001